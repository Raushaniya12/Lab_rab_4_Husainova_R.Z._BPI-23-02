<Window x:Class="Lab_rab_4_Husainova_R.Z._BPI_23_02.View.WindowNewEmployee"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab_rab_4_Husainova_R.Z._BPI_23_02.View"
        mc:Ignorable="d"
        Title="Новый сотрудник" Height="250" Width="500" MinHeight="250" MinWidth="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Код:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Name="TxtId" Text="{Binding Id, Mode=OneWay}" IsReadOnly="True" VerticalAlignment="Center"/>
        <Label Grid.Row="1" Grid.Column="0" Content="Должность:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="1" Grid.Column="1" Name="CbRole" 
          DisplayMemberPath="NameRole" 
          SelectedItem="{Binding RoleName, Mode=TwoWay}" 
          VerticalAlignment="Center"/>
        <Label Grid.Row="2" Grid.Column="0" Content="Фамилия:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Name="TxtLastName" Text="{Binding LastName, Mode=TwoWay}" VerticalAlignment="Center"/>
        <Label Grid.Row="3" Grid.Column="0" Content="Имя:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" Name="TxtFirstName" Text="{Binding FirstName, Mode=TwoWay}" VerticalAlignment="Center"/>
        <Label Grid.Row="4" Grid.Column="0" Content="Дата рождения:" VerticalAlignment="Center"/>
        <DatePicker Grid.Row="4" Grid.Column="1" SelectedDate="{Binding Birthday, Mode=TwoWay}" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Сохранить" Click="BtnSave_Click" Margin="5" MinWidth="100"/>
            <Button Content="Отменить" Click="BtnCancel_Click" Margin="5" MinWidth="100"/>
        </StackPanel>
    </Grid>
</Window>